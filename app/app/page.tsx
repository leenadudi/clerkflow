import { PageShell } from "@/components/page-shell";

export default function AppDashboardPage() {
  return (
    <PageShell
      title="Command center"
      description="Overdue FOIA, upcoming meetings, renewals, and board terms."
    />
  );
}
