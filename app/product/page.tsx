import { PageShell } from "@/components/page-shell";

export default function ProductPage() {
  return (
    <PageShell
      title="Product"
      description="Meetings, FOIA, municipal services, boards, and a resident hub — built for small-town clerks."
    />
  );
}
